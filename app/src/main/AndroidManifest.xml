<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifsul.vendas">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/shopping_cart"
        android:label="@string/app_name"
        android:roundIcon="@drawable/shopping_cart"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.CarrinhoActivity"
            android:label="Carrinho"
            android:parentActivityName=".activity.ProdutosActivity" ></activity>
        <activity
            android:name=".activity.ProdutoDetalheActivity"
            android:label="Produto" ></activity>
        <activity
            android:name=".activity.ClientesActivity"
            android:label="Clientes" ></activity>
        <activity
            android:name=".activity.ProdutosActivity"
            android:label="Produtos"
            android:theme="@style/AppTheme.NoActionBar" ></activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="Vendas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".barcode.BarcodeCaptureActivity"
            android:label="Leitor de código" ></activity>
        <activity
            android:name=".activity.ProdutoAdminActivity"
            android:label="ADM de produtos" ></activity>
        <activity
            android:name=".activity.SobreActivity"
            android:label="Sobre"></activity>
        <activity
            android:name=".activity.ClientesAdminActivity"
            android:label="ADM de clientes"></activity>
        <activity
            android:name=".activity.PedidosActivity"
            android:label="Pedidos"></activity>
    </application>

</manifest>